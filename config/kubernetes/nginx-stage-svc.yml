apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-stage-svc
  name: nginx-stage-svc
spec:
  ports:
    - 
      port: 80
      targetPort: 80
      protocol: TCP
      name: nginx
  clusterIP: 10.0.0.30
  selector:
    app: nginx-stage-rc